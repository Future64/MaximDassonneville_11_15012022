import '../Gallery/Gallery.css'
import Thumb from '../Thumb/Thumb'

const Gallery = () => {
    return ( 

        <div className = "Gallery" >
            {Thumb()} 
            {Thumb()}
            {Thumb()}
            {Thumb()}
            {Thumb()}
            {Thumb()}
            {Thumb()} 
            {Thumb()}
            {Thumb()}
            {Thumb()}
            {Thumb()}
            {Thumb()}
        </div>
    )
}

export default Gallery